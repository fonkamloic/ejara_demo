void main() {
  // testWidgets('AddPaymentMethod test', (WidgetTester tester) async {
  //   // Create an instance of the `AddPaymentMethod` class
  //   final addPaymentMethod = AddPaymentMethod();

  //   // Build the widget and add it to the tester
  //   await tester.pumpWidget(MaterialApp(home: addPaymentMethod));

  //   // Find the icon button that closes the screen
  //   final closeButton = find.byType(IconButton);
  //   // expect(closeButton, findsOneWidget);

  //   // Tap the close button
  //   await tester.tap(closeButton);

  //   // Verify that the screen is closed
  //   expect(find.byType(AddPaymentMethod), findsNothing);
  // });

  // // testWidgets('AddPaymentMethod test', (WidgetTester tester) async {
  // //   // Build the widget
  // //   await tester.pumpWidget(AddPaymentMethod());

  // //   // Find the switch in the widget tree
  // //   final switchFinder = find.byType(Switch);
  // //   // Check that the switch is present
  // //   expect(switchFinder, findsOneWidget);

  // //   // Find the switch's value
  // //   final switchValue = tester.firstState(switchFinder).value;
  // //   expect(switchValue, true);

  // //   // Simulate a tap event on the switch
  // //   await tester.tap(switchFinder);
  // //   await tester.pump();

  // //   // Check that the switch's value has been updated
  // //   final updatedSwitchValue = tester.firstState(switchFinder).value;
  // //   expect(updatedSwitchValue, false);
  // // });
}
